# CI Template (GitHub Actions)

Use a workflow that runs on push/PR for skill-related paths and executes:

- install dependencies
- `npm run skills:test`

Scope paths to skills/docs/scripts/tests so checks run only when relevant.
